<view class="home container">
  <sum-card 
    wx:for="{{articles}}" 
    wx:key="id" 
    tags="{{item.tags}}"
    title="{{item.title}}"
    createDate="{{item.createTime}}" 
    summary="{{item.summary}}"
    coverImgUrl="{{item.coverImgUrl}}"
  />
</view>